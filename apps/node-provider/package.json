{
  "name": "@cluster-apps/node-provider",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "vite --open",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check-types": "tsc --noEmit"
  }
}
