{
  "name": "@cluster-apps/global-registry",
  "private": true,
  "version": "0.8.1",
  "scripts": {
    "start": "vite --open",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check-types": "tsc --noEmit"
  }
}
