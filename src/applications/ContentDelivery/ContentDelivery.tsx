import { Typography } from '@developer-console/ui';
import { observer } from 'mobx-react-lite';

const ContentDelivery = () => <Typography>Content Delivery</Typography>;

export default observer(ContentDelivery);
