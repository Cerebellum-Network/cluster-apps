import { Typography } from '@developer-console/ui';
import { observer } from 'mobx-react-lite';

const ContentStorage = () => <Typography>Content Storage</Typography>;

export default observer(ContentStorage);
